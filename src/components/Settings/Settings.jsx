
import settingsStyle from './Settings.module.css';

function Settings() {
    return (
        <div>
            <div className={settingsStyle.section_title}>Settings block</div>
        </div>
    );
}

export default Settings;